image: gcc

build:
  stage: build
  before_script:
    - apt update && apt -y install cmake
  script:
    - mkdir build
    - /usr/bin/cmake -S . -B build
  artifacts:
    paths:
      - ./build

# run tests using the binary built before
test:
  stage: test
  script:
    - ./build/run_test
